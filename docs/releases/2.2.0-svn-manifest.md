# Release manifest — trill-ai-chat-lite 2.2.0

**Date:** 2026-06-21
**Git:** `feature/2.2-conversations` (base `e2ffe47` = 2.1.0 as shipped)
**Theme:** Conversations release (CNV-01..08)

## Pre-flight checklist (run in Local before touching SVN)

- [ ] `wp plugin activate trill-ai-chat-lite` on a clean install — no notices with `WP_DEBUG` on
- [ ] Schema migrates 1.3.0 → 1.4.0 on update: FULLTEXT index present (or `trcl_messages_fulltext` option = `0` on hosts without it, search still works via LIKE)
- [ ] Conversations smoke tests green: `wp eval-file tests/Conversations/DeleteByIdsSmoke.php`, `ConversationQuerySmoke.php`, `ExporterSmoke.php`
- [ ] Plugin Check against the **built ZIP**, not the working copy (scope trap: PC scans the installed plugin)
- [ ] Upgrade path: install 2.1.0 ZIP → use widget → update to 2.2.0 ZIP → Conversations page lists existing chats, no data loss
- [ ] `bin/build-assets.sh --check` → "all minified assets are up to date" (admin.js/.min regenerated)
- [ ] `wp i18n make-pot . languages/trill-ai-chat-lite.pot` re-run so the new 2.2 strings are captured

## Files CHANGED (copy over existing files in trunk/)

| File | Why |
|---|---|
| `trill-ai-chat-lite.php` | Version 2.2.0 (header + `TRCL_VERSION`) |
| `readme.txt` | Stable tag 2.2.0, "New in 2.2" block, changelog, upgrade notice |
| `includes/Database/Migrations.php` | Schema 1.4.0: FULLTEXT index on `trcl_messages.content` + `OPT_MESSAGES_FULLTEXT` flag (CNV-02) |
| `includes/Lite/UpgradeManager.php` | Runs the 1.4.0 migration on version change (CNV-01) |
| `includes/Admin/Admin.php` | Conversations submenu + `render_conversations`, `ajax_get_transcript`, `handle_conversations_export_post`, `handle_transcript_export_post`, `handle_conversation_delete_post` + `redirect_to_conversations`; delete confirm strings localised (CNV-03/04/08) |
| `includes/Gdpr/ConversationManager.php` | New public `delete_by_ids()` reusing the audited cascade (CNV-08) |
| `assets/css/admin.css` + `.min` | Conversations table, status/rating badges, transcript modal, check-column (CNV-03) |
| `assets/js/admin.js` + `.min` | Transcript modal (`.text()`-only) + delete actions (select-all + confirm) (CNV-03/08) |
| `languages/trill-ai-chat-lite.pot` | Regenerated — adds the 2.2 Conversations strings |

## Files NEW (add to trunk/)

| File | Why |
|---|---|
| `includes/Conversations/ConversationQueryService.php` | Read-only filtered/paginated query + transcript (CNV-01/02) |
| `includes/Conversations/TranscriptExporter.php` | CSV streaming — global filtered + per conversation (CNV-04) |
| `includes/Conversations/CsvCellSanitizer.php` | Formula/CSV-injection neutraliser used by the exporter (CNV-06) |
| `includes/Admin/views/conversations.php` | Conversations admin view: filters, table, pagination, transcript modal, delete form (CNV-03/08) |

## Files DELETED

None.

## NOT shipped (in `.distignore`)

`bin/`, `docs/` (including this manifest), `tests/`, `wporg-assets/`, dotfiles, root `*.md`.
Note: the three new `tests/Conversations/*Smoke.php` files are dev-only and excluded.

## SVN steps (your usual flow)

1. Copy the CHANGED + NEW files into `trunk/` per the tables above.
2. `svn cp trunk tags/2.2.0`.
3. Commit message suggestion: `Release 2.2.0 — conversations: admin list, filters + full-text search, transcript viewer, CSV export (injection-hardened), single/bulk delete; schema 1.4.0 FULLTEXT`.

## Translator note

New strings only (Conversations UI, filters, export, delete + confirm dialogs). No existing strings changed, so prior translations are unaffected; es_ES needs the new 2.2 strings translated.
