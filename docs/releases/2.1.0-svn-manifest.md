# Release manifest — trill-ai-chat-lite 2.1.0

**Date:** 2026-06-06
**Git:** `develop` @ `c926960` (merge of `feature/2.1-appearance`, base `598cf53` = 2.0.0 as shipped)
**Theme:** Appearance release (APP-01..08)

## Pre-flight checklist (run in Local before touching SVN)

- [ ] `wp plugin activate trill-ai-chat-lite` on a clean install — no notices with `WP_DEBUG` on
- [ ] Plugin Check against the **built ZIP**, not the working copy (scope trap: PC scans the installed plugin)
- [ ] Upgrade path: install 2.0.0 ZIP → settings as before → update to 2.1.0 ZIP → widget looks **identical** (defaults are visually silent)
- [ ] `bin/build-assets.sh --check` → "all minified assets are up to date"

## Files CHANGED (copy over existing files in trunk/)

| File | Why |
|---|---|
| `trill-ai-chat-lite.php` | Version 2.1.0 (header + `TRCL_VERSION`) |
| `readme.txt` | Stable tag 2.1.0, "New in 2.1" block, changelog, upgrade notice |
| `includes/Admin/Settings.php` | Appearance schema: 7 colours, dimensions, assistant name, avatar ID, font + launcher whitelists, `get_appearance_config()` |
| `includes/Admin/Admin.php` | wp-color-picker + wp.media enqueue on settings screen; `admin_post_trcl_reset_appearance` handler |
| `includes/Admin/views/settings.php` | Appearance tab added; position/colour/welcome moved out of General |
| `includes/Frontend/Frontend.php` | `build_appearance_inline_css()` (full `--trcl-*` var block, 4-corner rules, font), assistant name + avatar + launcher style in localize, dynamic greeting |
| `assets/css/chat-widget.css` + `.min` | Consumes CSS vars; bubble launcher; expand button + expanded state; mobile hides expand |
| `assets/css/chat-launcher.css` + `.min` | Bubble launcher styles (kept in sync with widget CSS) |
| `assets/css/admin.css` + `.min` | Appearance tab layout, position/launcher grids, avatar control, live preview (`trcl-pv-*`) |
| `assets/js/chat-widget.js` + `.min` | Conditional launcher markup, dynamic avatar (escaped), expand/collapse toggle |
| `assets/js/chat-launcher.js` + `.min` | Conditional bubble launcher in bootstrap (sync with widget render) |
| `assets/js/admin.js` + `.min` | Colour pickers, slider read-outs, avatar picker (wp.media), live preview bindings |
| `languages/trill-ai-chat-lite.pot` | Regenerated — 314 strings; greeting/typing now `%s`-parameterised |

## Files NEW (add to trunk/)

| File | Why |
|---|---|
| `includes/Admin/views/settings-appearance.php` | Appearance tab view: colours, position & dimensions, identity (name/welcome/avatar/font), reset form, live preview |

## Files DELETED

None.

## NOT shipped (in `.distignore`)

`bin/`, `docs/` (including this manifest), `wporg-assets/`, dotfiles.

## SVN steps (your usual flow)

1. Copy the CHANGED + NEW files into `trunk/` per the tables above.
2. `svn cp trunk tags/2.1.0`.
3. Commit message suggestion: `Release 2.1.0 — appearance: colour palette, positions, dimensions, assistant identity, avatar, font, live preview, desktop expand, configurable launcher`.

## Translator note

`"Hi there! I'm Robin, ..."` and `"Robin is typing..."` were replaced by
`%s`-parameterised strings. Existing translations of the old literals are
orphaned — es_ES needs the two new strings translated.
